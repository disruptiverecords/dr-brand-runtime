# Kinetic Editorial+ — the judged build direction (June 2026)

**Chassis: Prompt B (Kinetic Editorial), enriched with judge-mandated grafts. Tournament: 4-lens judge panel + 3 adversarial skeptics (7 agents). C won on points (32.5) but was unanimously refuted; B promoted (30.5).**

## The concept
One continuous color-graded film, no WebGL. GSAP ScrollTrigger + Lenis. Everything animated IS the content. Jury story: "restraint as spectacle — the calmest site you cannot stop scrolling."

## Core (from B)
- Master background timeline: tone blocks BLEND AT THE SEAMS (scroll-linked gradient bridging) — hard block identities preserved (brand-purist condition), grain persistent.
- Per-ring SVG vortex as scroll protagonist: rings wind, split, re-gather between sections (existing component).
- Catalogue: pinned horizontal gallery, parallax artwork, ticking mono metadata, clip-path reveals. Mobile = native scroll-snap.
- Shared-element artwork continuity (hovered artwork tints next section).
- Masked line reveals w/ per-word stagger.

## Grafts from A (doctrine, not WebGL)
- THE STREAMS MOMENT: vortex unwinds into a rising line via SVG path interpolation — the 1B stat becomes a quiet consequence of the brand mark, not a data-viz set piece (solves Ivo's "too show-off" note).
- HARD RULE: scroll position always owns every timeline. No scroll-jacking, ever.
- Per-section perf gates in Playwright rounds: 60fps desktop / 40fps mid-mobile, DevTools traces.
- Tiered degradation ladder: full → no-pin (touch) → intersection-only (reduced-motion).

## Grafts from C (audio as content, analyser killed)
- Tap/click-to-hear 15s previews on releases (own rights, 96kbps AAC, lazy) — the #1 comprehension task for a music company (UX-skeptic's top-scored feature).
- Glass mini-player pill (DisruptiveOS recipe), Monument Mono metadata. Gesture-gated; mono sound toggle in nav.
- NO FFT/spectrum analyser (= graphic equalizer = rejected EDM register; also technically false claim — wave is a global scalar). At most: one RMS scalar gently modulating the EXISTING ambient wave inside its approved envelope. Prototype for Ivo approval BEFORE committing (skeptic-3 condition).

## Explicit kills
- Morphing custom cursor (2021 cliché — awards juror).
- Lime cursor state (off-palette neon door — brand purist).
- Live AnalyserNode visuals (non-deterministic, unverifiable in Playwright — skeptic 2).
